# Copy to .env on the server (chmod 600). Never commit real secrets.

MYSQL_PASSWORD=generate-long-random-password
MYSQL_ROOT_PASSWORD=generate-long-random-root-password

DB_HOST=db
DB_PORT=3306
DB_USER=mubsme
DB_PASSWORD=generate-long-random-password
DB_NAME=mubs_super_admin

# Keep the production JWT_SECRET when importing an existing database.
JWT_SECRET=at-least-32-characters-from-previous-server

NEXT_PUBLIC_API_URL=https://mubsme.mubs.ac.ug
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_SECURE=true
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM="MUBS M&E System <mubsme@mubs.ac.ug>"

HRMS_API_URL=https://hrms.mubs.ac.ug
HRMS_API_KEY=
HRMS_API_SECRET=
HRMS_TIMEOUT_MS=20000
REQUIRE_AUTH=true
SDS_HRM_PULL_SECRET=
