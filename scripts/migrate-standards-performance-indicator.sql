-- Adds a Standard-level performance indicator field.
-- Run once on MySQL/MariaDB.
ALTER TABLE `standards`
  ADD COLUMN `performance_indicator` varchar(512) DEFAULT NULL;

-- Adds a Standard-level performance indicator field.
-- Run once on MySQL/MariaDB.
ALTER TABLE `standards`
  ADD COLUMN `performance_indicator` varchar(512) DEFAULT NULL;

-- Adds a Standard-level performance indicator field.
-- Run once on MySQL/MariaDB.
ALTER TABLE `standards`
  ADD COLUMN `performance_indicator` varchar(512) DEFAULT NULL;

